## Orkestra

Our orchestrator is limited to deal with docker containers.

The main purpose of an orchestration system is to accept tasks from users and run them on the system’s worker nodes. 

- Docker SDK (Go client)